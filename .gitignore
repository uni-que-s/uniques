# dependencies
node_modules/

# build output
dist/
server/dist/
web/dist/
*.tsbuildinfo

# local SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm
data/

# scan output
*.sarif

# logs & os cruft
*.log
.DS_Store

# local editor / agent tooling config
.claude/

# secrets & credentials — never commit real keys/tokens.
# (the intentional vulnerable fixtures under server/sample-target/ are
#  already tracked and are explicitly re-included below.)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa
id_ed25519
credentials.json
secrets.json
!server/sample-target/**
