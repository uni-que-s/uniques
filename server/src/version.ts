/** Single source of truth for the tool version stamped into exports (CBOM tool
 *  metadata, OpenAPI, etc.). 0.2.3 = skip non-production dirs (tests/examples/
 *  samples) so the grade reflects shipped code, + on-prem positioning. */
export const VERSION = "0.2.3";
