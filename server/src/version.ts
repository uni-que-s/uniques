/** Single source of truth for the tool version stamped into exports (CBOM tool
 *  metadata, OpenAPI, etc.). 0.2.2 = detection precision (comment masking) +
 *  honesty fixes (control-gap framing, no self-asserted "pass", tunable weights). */
export const VERSION = "0.2.2";
