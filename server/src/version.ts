/** Single source of truth for the tool version stamped into exports (CBOM tool
 *  metadata, OpenAPI, etc.). 0.2.4 = a clean scan (zero findings) now passes
 *  inventory controls, so every framework is 100% on a clean codebase. */
export const VERSION = "0.2.4";
