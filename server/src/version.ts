/** Single source of truth for the tool version stamped into exports (CBOM tool
 *  metadata, OpenAPI, etc.). 0.2.1 adds embedded C/C++ firmware crypto detection
 *  (mbedTLS / wolfSSL / OpenSSL C API). */
export const VERSION = "0.2.1";
