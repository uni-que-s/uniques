/** Single source of truth for the tool version stamped into exports (CBOM tool
 *  metadata, OpenAPI, etc.). 0.3.0 = per-occurrence context classifier (ENG-01a):
 *  a crypto name in a prose string (log/error/doc) is reclassified as a possible
 *  mention, not a use. Builds on 0.2.5's per-finding confidence score. */
export const VERSION = "0.3.0";
