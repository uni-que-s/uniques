/** Single source of truth for the tool version stamped into exports (CBOM tool
 *  metadata, OpenAPI, etc.). 0.2.5 = per-finding confidence score; low-confidence
 *  "possible mentions" are surfaced separately and excluded from the grade. */
export const VERSION = "0.2.5";
